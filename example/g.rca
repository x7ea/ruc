enum Result<T, E> {
    ok: T,
    err: E
}

fn unwrap_or<T, E>(this: Result<T, E>, alt: T) {
    if let v = this.ok then v else alt
}

fn main() {
    let value = new Result<Int, Str>
    let proc = unwrap_or<Int, Str>
    let alt = -1

    let value.ok = 3
    print proc(value, alt)

    let value.err = "Err"
    print proc(value, alt)
}
