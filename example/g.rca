enum Result<T, E> {
    ok: T,
    err: E
}

fn unwrap_or<T, E>(this: Result<T, E>, alt: T) {
    if let v = this.ok then v else alt
}

fn main() {
    let value = new Result<Float, Str>
    let proc = unwrap_or<Float, Str>
    let alt = -1.0

    let value.ok = 3.0
    print proc(value, alt)

    let value.err = "Err"
    print proc(value, alt)
}
