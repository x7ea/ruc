fn reduce<T>(this: [T], lambda: T(T, T)) {
    let index = 1
    while index < this.len do {
        let this[0] = lambda(this[0], this[index])
        let index = index + 1
    }
    this[0]
}

fn add<T>(a: T, b: T) a + b

fn main() {
    let x = [8, 5, 4]
    print reduce<Int>(x, add<Int>)

    let y = [1.2, 2.3, 3.3,3.3]
    print reduce<Float>(y, add<Float>)
}
