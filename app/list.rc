use std

struct List<T> {
    car: T,
    cdr: List<T>
}

fn List<T>(value: T): List<T> {
    let this = new List<T>
    let this.car = value
    this
}

fn List.link<T>(a: List<T>, b: List<T>) {
    while a.cdr? do let a = a.cdr
    let a.cdr = b
}

fn main() {
    let a = List<Int>(100)
    let b = List<Int>(200)
    let c = List<Float>(300.0)
    a.link(b)
    b.link(c)

    print a.cdr.car
}
