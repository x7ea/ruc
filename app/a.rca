enum Expr {
    add: Term,
    number: Int
}

fn Expr.eval(this: Expr) {

}

fn main() {
    let i = 0
    while i < 100 do {
        let i = i + 1

        let this = new State
        let this.number = i
        if i % 3  == 0 then let this.fizz = ()
        if i % 5  == 0 then let this.buzz = ()
        if i % 15 == 0 then let this.fizzbuzz = ()


    }
}
