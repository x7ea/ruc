enum Peano {
    Zero: (),
    Succ: Peano
}

fn add(lhs: Peano, rhs: Peano) -> Peano {
    match rhs {
        Zero = clone lhs,
        Succ n = Peano::Succ(add(lhs, n))
    }
}

fn decode(this: Peano) -> Int {
    let digit = 0
    if let n = this.succ then {
        return decode(n) + 1
    }
    digit
}

fn main() {
    let one = Peano::Succ(Peano::Zero)
    let proof = one.add(one)
    print proof.decode()
}
